export default function (path) {
  if (!path) return
  if (!(typeof path === 'string')) return
  let split = /\//.test(path) ? '/' : '\\'
  let splitArr = path.split(split)
  return splitArr[splitArr.length - 1]
}
