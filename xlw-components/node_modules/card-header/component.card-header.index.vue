<template>
  <div class="component-title">
    <span class="title">{{title}}</span>
    <div class="buttons">
      <slot></slot>
    </div>
  </div>
</template>
<style lang="scss">
  @import "card-header.theme";
  .component-title{
    height: $card-header-button-height;
    line-height:$card-header-button-height;
    .title{
      font-size:$card-header-title-font-size;
      font-weight:$card-header-title-font-weight;
      float:left;
    }
    .buttons{
      float:right;
      > * {
        margin-left:$card-header-button-margin-left;
        display:inline-block;
      }
    }
  }
  .el-card__header{
    padding: #{$card-header-padding - 2 $card-header-padding};
  }
  .el-card__header + .el-card__body{
    height:calc(100% - #{2 * $card-header-padding - 2 + $card-header-button-height});
    box-sizing:border-box;
    overflow:auto;
  }
</style>
